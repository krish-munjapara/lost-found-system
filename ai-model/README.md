# Guardian-Link AI Model Directory
# ──────────────────────────────────────────────

# This directory is reserved for trained AI models.
# The face matching system uses DeepFace with the Facenet model,
# which automatically downloads model weights on first use.

# If you train a custom model, save it here as:
#   trained_model.h5    (Keras/TensorFlow format)
#   trained_model.pt    (PyTorch format)

# The pre-trained Facenet model weights will be cached by DeepFace
# in the ~/.deepface/weights/ directory automatically.
